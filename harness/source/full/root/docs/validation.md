# 검증 기록과 기준

이 문서는 프로젝트에서 자주 쓰는 검증 명령과 수동 확인 기준을 정리한다. 개별 작업의 실제 실행 결과는 관련 ExecPlan의 `검증 기록`에도 남긴다.

## 기본 명령

| 목적 | 명령 | 비고 |
| --- | --- | --- |
| 테스트 | TBD | 예: `npm test`, `pytest`, `go test ./...` |
| 린트 | TBD | 예: `npm run lint` |
| 빌드 | TBD | 예: `npm run build` |
| 타입 검사 | TBD | 예: `npm run typecheck` |

## 변경 유형별 기준

### 제품 기능

- 관련 product spec의 수용 기준을 확인한다.
- happy path, 빈 상태, 오류 상태, 권한 경계를 검증한다.
- 사용자에게 보이는 문구와 상태가 spec과 일치하는지 확인한다.

### API 또는 데이터 변경

- 입력 validation과 오류 응답을 확인한다.
- 마이그레이션, rollback, backward compatibility를 확인한다.
- 인증과 인가 경계를 확인한다.

### UI 변경

- 키보드 조작, focus 표시, 스크린 리더 label을 확인한다.
- 작은 화면과 긴 텍스트에서 겹침이나 잘림이 없는지 확인한다.
- 필요한 경우 프로젝트별 접근성 체크리스트를 별도 문서로 추가한다.

### 보안 관련 변경

- secret이 코드와 로그에 노출되지 않는지 확인한다.
- dependency 변경이 있으면 audit을 실행한다.
- 필요한 경우 프로젝트별 보안 체크리스트를 별도 문서로 추가한다.

### 성능 관련 변경

- 최적화 전후 측정값을 비교한다.
- p95 또는 p99 latency를 확인한다.
- 필요한 경우 프로젝트별 성능 체크리스트를 별도 문서로 추가한다.

## 검증 로그

| 날짜 | 변경 또는 ExecPlan | 실행한 명령 | 결과 | 남은 위험 |
| --- | --- | --- | --- | --- |
| TBD | TBD | TBD | TBD | TBD |
